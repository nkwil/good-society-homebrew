Post-MVP sweep + dossier polish + letter composer overhaul

Massive batch landing the post-MVP design patch (batches 1-11 + deferred items)
plus iteration polish on the dossier, letter composer, and connection sheet.

POST-MVP SWEEP (per docs/design/post-mvp-design-patch.md):
  1.  Theme registry overhaul — Mags → Secret, Pearlinda added (7 Majors,
      5 Connection variants, 1 NPC). Dark-theme variant slots for legible
      reputation tags on dark paper.
  2.  Cross-cutting primitives — module/constants.js with SEAL_TYPES,
      COWORK_SURFACES, CHROME_ICONS registries. profilePic helper. New
      eyebrow + empty-state + persona-switcher primitives.
  3.  Dashboard frame + dock parity — sage hairline panel, condition
      sub-rail mirrors dashboard, dark-theme rep colors flow through.
  4.  Journal organization — letterFolder / monologueFolder /
      sessionLogFolder / cycleReflectionFolder, entryType flags.
  5.  Token hover card v2 — Connection/NPC subtitle field, HTMLField for
      hoverSummary, world toggle for Major auto-summary.
  6.  World identity — Arrival app (empty-canvas state), pause overlay,
      shared visual register. Six new settings.
  7.  Foundry chrome icons — CHROME_ICONS registry feeds scene controls,
      sidebar tabs, journal entries. Body-class gated.
  8.  Cabinet — right-edge player module menu, rail + drawer, per-user
      visibility settings.
  9.  Epistolary seal-type registry + burn-after-reading hook.
  10. Token spend events scaffolding — discardResolve/handoffResolve,
      monologue overlay, journal archive, three new settings.
  11. Cycle dividers + Novel Reader settings.

DEFERRED ITEMS (follow-up session):
  - Dossier refactor (additive book.hbs alt-spreads, ESC navigation)
  - Hover-card render path consumes new schema fields
  - Monologue scene-freeze overlay (frameless, socket-synced, GM commit)
  - Epistolary Wizard + Roster (3 tabs, GM-only roster, phase auto-open)
  - Novel Reader app (920×780, cover + reader modes, per-user state)

ITERATION POLISH (this session):
  - Connection + NPC sheets restyled as single-page dossiers
  - 12-theme picker on Connection sheets + persona switcher
  - Title/subhead (bio.title) field on all three sheet types, wired
    through dossier, dashboard, dock, hover card
  - Pronouns system (bio.pronouns + parsePronouns helper) with
    propagation to system-emitted placeholders
  - Responsive cameo name scaling (measurement-based, SCALE_MIN=0.30)
  - Backstory: auto-paginated two-page spreads with column flow,
    plain-text-to-paragraph normalization, HTML <hr> separators
  - Textarea + format-button editors (reverted from ProseMirror dialogs)
  - Letter Composer: two-page dossier layout, parchment chat card,
    greeting + closing pickers (Regency salutations), 15-asset wax-seal
    registry replacing the 3 seed seals
  - Connection sheet polish: linked Major picker (DialogV2), description
    line-break normalization, themed impression cards (full Major palette
    cascades), frameless 56px impression portraits
  - Major sheet connection chips: dropped description + hairline frame
  - Public-info attribution fix: "FROM X" on impressions uses true
    identity name, never the active persona name (2026-05-10)
  - Persona-fallback bug fixed across profilePic/dashboard/dock contexts:
    only EXPLICIT persona selection shows persona name + image
  - migrateData partial-update bug: every actor data-model now gates
    coercion on 'field in source' so persona swaps don't stomp theme
  - Seal-disc chat-card indicator pulls iconAsset via CSS variable
  - Speaking-as bar persona-aware name resolution

VISUAL POLISH:
  - Dixon / Pearlinda / Clayton surface colors muted further
  - Foundry chrome theme (body.gs-chrome-themed) refined
  - Letter card as universal cream stationery (theme drives accents only)
  - Connection chips: identity-only, themed paper signals tappable

CHANGES: 217 files / +29884 / -1750
